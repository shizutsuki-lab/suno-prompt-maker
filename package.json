{
  "name": "suno-prompt-maker-netlify",
  "version": "1.0.0",
  "private": true,
  "description": "SUNO PROMPT MAKER - Netlify deployment (server-side generation, no client-exposed API key)",
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  }
}
